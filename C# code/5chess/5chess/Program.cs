namespace _5chess
{
    class Program
    {
        static void Main(string[] args)
        {
            string[][] board;

            int x = 0;
            int y = 0;
            int dx = 0;
            int dy = 0;

            for (int = 0; )

            // drawboard(); while loop

            public static void createBoard()

            {// board = new string[8][]; // column creation

            if (int = i < 2 || i > 5)
        
                for (int i = 0; i < 8; i++)
                {
                
                }

            public static void drawboard()
            
            System.Console.Write();
            System.Console.Clear();

            // create class for creating the board
            // add how board is created into it
            // include a constructor into it

        // now have to have pieces move (which piece and where to go)
        // using the coordinates (int x, int y, int dx, int dy)
        // change x's to blanks and add new x to a new location




            
        }
    }
}
